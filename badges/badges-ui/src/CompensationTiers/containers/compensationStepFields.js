import {connect} from  'react-redux';
import CompensationStepFields from '../views/compensationStepFields';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({});

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(CompensationStepFields);
